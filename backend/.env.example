# Example backend env
PORT=3000
CLIENT_URL=http://localhost:5173
JWT_SECRET=replace_this_with_a_secure_random_value
# Note: Do NOT commit secrets to the repository. Add `.env` to your local .gitignore.
